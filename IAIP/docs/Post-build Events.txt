I made this file because editing these lines is easier here than in the tiny input 
box for these commands. Nothing in this file gets executed. You have to copy these
commands into the right place. Follow instructions below. 

— Doug

------------------------------

Here are the Post-build event command line(s) for the main project. To use, copy 
the following in its entirety (between the "-----" lines) and paste into the box at:

[Project → Properties → Compile → Build events… → Post-build event command line]

----- Start here -----

if $(ConfigurationName) == Debug (
REM Debug build -----
C:\Users\dwaldron\AppData\Local\Pandoc\pandoc.exe -f markdown -t html5 -s -o "$(TargetDir)docs\ABOUT.html" -c "style.css" -A "$(TargetDir)docs\scripts.html" "$(TargetDir)docs\ABOUT.md"
C:\Users\dwaldron\AppData\Local\Pandoc\pandoc.exe -f markdown -t html5 -s -o "$(TargetDir)docs\CHANGELOG.html" -c "style.css" "$(TargetDir)docs\CHANGELOG.md"
) ELSE (
REM Release build -----
C:\Users\dwaldron\AppData\Local\Pandoc\pandoc.exe -f markdown -t html5 -s -o "$(TargetDir)docs\ABOUT.html" -c "style.css" -A "$(TargetDir)docs\scripts.html" "$(TargetDir)docs\ABOUT.md"
C:\Users\dwaldron\AppData\Local\Pandoc\pandoc.exe -f markdown -t html5 -s -o "$(TargetDir)docs\CHANGELOG.html" -c "style.css" "$(TargetDir)docs\CHANGELOG.md"
del "$(TargetDir)docs\*.md"
del "$(TargetDir)docs\scripts.html"
copy /b "$(TargetDir)IAIP-Updater\IAIP.update.exe" "$(TargetDir)IAIP-Updater\IAIP.update2_6_4.exe"
)

----- End here -----

And here are the Post-build events command line(s) for the installer project. To use, 
copy the following in its entirety (between the "-----" lines) and paste into the box at:

[IaipInstaller → Properties (tab) → PostBuildEvent]

Currently none!
----- Start here -----


----- End here -----
